// go.js by qxxst
import { sourceFileOwned } from 'lib/util.js';
/** @param {NS} ns */
export async function main(ns: any) {
	const desiredOpponent: string = "Illuminati";

    let canCheat: boolean = false;
    if (sourceFileOwned(ns, 14, 2)) {
        canCheat = true;
    }
}